# frontend/.env

# アプリケーション名 (任意、必要なら)
VITE_APP_NAME="Terrace App"

# バックエンドAPIのベースURL (重要)
# バックエンドの APP_URL + "/api" に相当
VITE_API_URL="https://terrace.aqsh.co.jp/api"

# アプリケーションのベースURL (重要)
# CSRF Cookie 取得などで使用 (バックエンドの APP_URL と同じ)
VITE_APP_URL="https://terrace.aqsh.co.jp"

# --- Pusher.com 関連の設定 ---
# (バックエンドの Pusher 設定と同じ値を設定)
VITE_PUSHER_APP_KEY="c2bd2f655aa4082de34b"
VITE_PUSHER_CLUSTER="ap3"
VITE_PUSHER_SCHEME="https" # 通常は "https" (wss)

# 以下の Pusher 変数は通常 Echo 側で直接指定しなくても良い場合が多い
# VITE_PUSHER_HOST=      # Pusher.com の標準エンドポイントを使う場合は空
# VITE_PUSHER_PORT=443   # 通常は空でOK

# --- 不要な Reverb 関連 (削除またはコメントアウト) ---
# VITE_REVERB_APP_KEY=...
# VITE_REVERB_HOST=...
# VITE_REVERB_PORT=...
# VITE_REVERB_SCHEME=...

# --- その他のフロントエンド固有の環境変数があればここに追加 ---
# 例: Google Analytics ID など
# VITE_GA_ID=G-XXXXXXXXXX